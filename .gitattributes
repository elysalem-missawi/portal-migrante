# Normalize line endings (keep LF in repo)
* text=auto eol=lf

# Mark binaries
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary

# SVGs تُعامل كنص (تعمل diff)
*.svg text

# Windows shell/batch
*.cmd text eol=crlf
*.bat text eol=crlf
